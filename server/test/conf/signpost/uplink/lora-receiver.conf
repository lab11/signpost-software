#
# Config file for lora translation
#

# Where the lora mqtt stream is running.
incoming_host = localhost
incoming_port = 1882
incoming_protocol = mqtt

# Credentials
incoming_username = lora_username
incoming_password = lora_password

#outgoing port
outgoing_port = 1883
